{
  "name": "number-base-converter",
  "version": "1.0.0",
  "description": "A tool for converting numbers between different bases.",
  "scripts": {
    "test": "jest",
    "start": "node index.js"
  },

  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0"
  }
}
